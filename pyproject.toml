[project]
name = "ai-agent-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-genai==1.12.1",
    "mypy>=1.16.0",
    "pip>=25.1.1",
    "python-dotenv==1.1.0",
]
